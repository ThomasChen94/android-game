<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
            <Button
                android:gravity="center|left"
                android:drawableRight="@drawable/forward_icon"
                android:background="@drawable/button_border"
                android:layout_width="180dp"
                android:layout_height="wrap_content"
                android:onClick="onclickTrigger"
                android:drawableLeft="@drawable/onclick_icon"
                android:text="onclick"/>
            <Button
                android:gravity="center|left"
                android:drawableRight="@drawable/forward_icon"
                android:background="@drawable/button_border"
                android:layout_width="180dp"
                android:layout_height="wrap_content"
                android:onClick="onenterTrigger"
                android:drawableLeft="@drawable/onenter_icon"
                android:text="onenter"/>
            <Button
                android:gravity="center|left"
                android:drawableRight="@drawable/forward_icon"
                android:background="@drawable/button_border"
                android:layout_width="180dp"
                android:layout_height="wrap_content"
                android:onClick="ondropTrigger"
                android:drawableLeft="@drawable/ondrop_icon"
                android:text="ondrop"/>
        </LinearLayout>
    </ScrollView>

</LinearLayout>